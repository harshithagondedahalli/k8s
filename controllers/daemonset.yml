apiVersion: apps/v1
kind: Daemonset
metadata:
  name: mydaemon
spec:
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      conatiners:
      - name: mycont
        image: nginx:1.22.1
        ports:
        - containerPort: 80