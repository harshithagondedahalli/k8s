apiVersion: apps/v1
kind: Pod
metadata:
  name: nginx-deploy
spec:
  replicas: 4
  template:
    metadata:
      lables:
      app: nginx
  spec:
    containers:
    - name: nginx
    ports:
    - containerport: 80
  slector:
    app: nginx