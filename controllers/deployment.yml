apiVersion: apps/v1
kind: Deployment
metadata:
  name: mypod
  labels:
    app: my-deployment

spec:
  replicas: 4
    selector:
      matchLabels:
        app: my-deployment
spec:
  containers:
  - name: deploymentcont
    image: nginx
    ports:
    - containerPort: 80