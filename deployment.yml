apiVersion: apps/v1
kind: Pod
metadata:
  name: nginx deployment
spec:
  replica: 4
  template:
    metadata:
      labels:
      app: my-nginx
  spec:
    containers:
      - name: nginx
        image: nginx:1.22.1
        ports:
          - containerport: 80

  selector:
    app: my-nginx